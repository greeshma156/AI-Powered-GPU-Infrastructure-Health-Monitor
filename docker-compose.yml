services:

  # GPU Telemetry Simulator + Prometheus Exporter
  gpu-simulator:
    build: ./simulator
    container_name: gpu-simulator
    ports:
      - "8000:8000"       # Prometheus metrics endpoint
    environment:
      - NUM_GPUS=4        # Number of simulated GPUs
      - SCRAPE_INTERVAL=15
    networks:
      - monitoring

  # Prometheus — scrapes metrics every 15s
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus/alert_rules.yml:/etc/prometheus/alert_rules.yml
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.enable-lifecycle"
    networks:
      - monitoring

  # Alertmanager — handles firing alerts
  alertmanager:
    image: prom/alertmanager:latest
    container_name: alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml
    networks:
      - monitoring

  # Grafana — dashboards and visualisation
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    depends_on:
      - prometheus
    networks:
      - monitoring

  # Elasticsearch — stores diagnostic logs
  # elasticsearch:
    # image: docker.elastic.co/elasticsearch/elasticsearch:8.13.0
    # container_name: elasticsearch
    # environment:
  #     - discovery.type=single-node
  #     - xpack.security.enabled=false
  #     - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
  #   ports:
  #     - "9200:9200"
  #   volumes:
  #     - elasticsearch_data:/usr/share/elasticsearch/data
  #   networks:
  #     - monitoring

  # # Logstash — ingests and transforms logs
  # logstash:
  #   image: docker.elastic.co/logstash/logstash:8.13.0
  #   container_name: logstash
  #   ports:
  #     - "5044:5044"
  #     - "5000:5000/udp"
  #   volumes:
  #     - ./logstash/pipeline:/usr/share/logstash/pipeline
  #   depends_on:
  #     - elasticsearch
  #   networks:
  #     - monitoring

  # # Kibana — log visualisation
  # kibana:
  #   image: docker.elastic.co/kibana/kibana:8.13.0
  #   container_name: kibana
  #   ports:
  #     - "5601:5601"
  #   environment:
  #     - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
  #   depends_on:
  #     - elasticsearch
  #   networks:
  #     - monitoring

volumes:
  prometheus_data:
  grafana_data:
  # elasticsearch_data:

networks:
  monitoring:
    driver: bridge
